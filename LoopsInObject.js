 const myObject ={
    frontend: "html",
    backend: "Sql",
    Scripting: "javascript"
 }

 for (const key in myObject) {
    
    console.log(`The values in the objects are ${myObject[key]}`);
 }
 // to print only the keys

 